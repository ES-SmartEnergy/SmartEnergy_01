import React, { useState, useEffect } from "react";
import axios from "axios"

function Home() {
  /*useEffect(() => {
    axios
      .get("http://localhost:3000")
      .then(() => {
        console.log("Deu certo");
      })
      .catch(() => {
        console.log("Deu errado");
      });
  });*/

  return (
    <dev>
      <p>Aqui estará um conteudo de Home</p>
    </dev>
  );
}

export default Home;
